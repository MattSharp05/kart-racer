// Storage (MK-37): `store` (localStorage or memory), `settings` (versioned), `prefs` (last picks)
// and `records` (best times). Import the module you need; this index keeps older imports working.
export * from './prefs';
export * from './records';
export * from './settings';
export * from './store';
